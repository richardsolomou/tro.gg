# Copy to client/.env. The Colyseus URL defaults to the local server. PostHog is
# a no-op when the key is unset, so it's optional for a local loop.
VITE_COLYSEUS_URL=ws://localhost:2567
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
