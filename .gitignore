node_modules/
# pnpm is the only package manager (see packageManager in package.json); keep
# stray npm/yarn lockfiles out so they can't drift from pnpm-lock.yaml.
package-lock.json
yarn.lock
.env*
!.env.example
!**/.env.example
dist/
*.tsbuildinfo
.DS_Store

# Per-server database identity, resolved on first publish.
spacetime.local.json

# Playwright e2e artifacts.
test-results/
playwright-report/
blob-report/
playwright/.cache/
