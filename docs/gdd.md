# tro.gg — Game Design Document

The buildable spec for tro.gg. If you are an agent working on this codebase: this file is the source of truth for game rules, data, naming, and scope, together with [analytics.md](analytics.md) (events and flags) and [world.md](world.md) (setting, tone, and naming for UI copy). [challenge.md](challenge.md) is human-facing background; you rarely need it. When code and this document disagree, flag the conflict — don't silently pick one.

## How to use this document

- **Glossary names are canonical.** Use them in code, schema, events, and UI. Don't invent synonyms (it's a `node`, not a "resource spawner").
- **Constants marked *(initial)* are starting values.** Keep them centralized in shared constants; make them remotely configurable only when runtime tuning or experiments are useful.
- **The invariants section is non-negotiable.** If a task seems to require breaking one, stop and ask.
- **Roadmap notes are planning context, not gates.** Do not block a task just because it used to belong to a later phase.
- **Custom analytics events and feature flags are registered in [analytics.md](analytics.md)** in the same change that introduces or changes them.

## Glossary

| Term | Meaning |
| ---- | ------- |
| trogg | A player character. Lowercase in prose, `player` in code/schema. |
| zone | One contiguous area of the world. The unit of subscription, rendering, and chat. |
| tile | The grid unit. Positions are integer tile coordinates within a zone. |
| node | A gatherable world object (rock, mushroom). Has a type, a state, and a respawn timer. |
| boulder | A mineable rock. Sits on an unwalkable tile; a pickaxe mines it into Stone. `boulder` in code/schema. |
| tree | A choppable tree. Sits on an unwalkable tile like a boulder; an axe fells it into Wood. `tree` in code/schema. |
| interact | The generic action key (`E`). It picks up ground items and picks up / puts down Hogs; pickup scans adjacent tiles and prioritises the tile the trogg faces, while put-down uses the faced tile first. Future effects (switch, fire) hang off the same key. |
| carry | A trogg holding a tile-sized object (a Hog) on its person — the object leaves its tile until put down. `carrying` in code/schema. |
| action | A timed activity a player starts on a node (mine, forage). One action at a time per player. |
| skill | A progression track (mining, foraging). XP and levels per skill, per player. |
| recipe | A crafting definition: inputs → output, skill requirement. |
| project | A communal construction: the tribe pools resources to build it; completing it unlocks something. |
| Hog | A friendly hedgehog NPC (merchant, townsfolk, protectee). See [world.md](world.md). |
| guest | An anonymous player (self-issued anonymous session). Has a generated name until upgrade. |

## Design pillars

1. One mechanic shipped at a time. The game is always playable.
2. Players are residents, not spectators. Guests join in seconds, no signup wall.
3. Instrumentation is first-class — the dashboards are part of the project, built in the open. Experiments are run transparently: players are told when they're in one.
4. One shared world. No instancing below ~1,000 concurrent in a zone; scaling problems are solved when the graphs demand it, not in advance.
5. Programmer low-poly 3D for now; visual polish and art direction are deferred.

## Core loop

Valheim-grammar progression: gather → craft better tools → better tools reach harder resources → repeat outward into new areas.

1. Land on tro.gg → exist in the world within seconds as a guest (no signup wall).
2. Click a tile (or use WASD) → walk there. Click a node → walk there, then start the action.
3. Action completes → items + XP. Node depletes, respawns later.
4. Craft tools and goods at the Hog town; contribute resources to communal projects that unlock new areas.
5. Chat renders as speech bubbles over heads (and in a side panel).

**Planned onboarding:** new players spawn in the shared starting zone (working name: the cave) and navigate to a checkpoint, which unlocks the hub (`hog-town`). It teaches movement and the world before the full loop opens. The specific framing is deferred (see [world.md](world.md)).

## Systems

### Movement

Two input modes, both supported:

- **Click-to-move:** click a tile → the trogg walks a pathfound route to it. Click a node or obstacle → route to the nearest reachable tile beside it (get as close as possible), then act.
- **WASD / arrow keys:** hold to move, release to stop. **Free camera-relative movement:** W walks where the camera looks, S backs up, A/D strafe, and held keys combine (W+D walks the forward-right diagonal at the same overall speed — the shared projection normalises velocity). A press moves (and faces) immediately — no turn-in-place beat — and a trogg can rest anywhere, not just on tile centres. Turning the camera mid-walk curves the walk with it: the client re-maps the held intent when the camera heading crosses a 15° bucket (still input-driven — a drag is input; never per-frame resends of an unchanged heading). On the wire a heading is an integer vector with axes in ±`DIR_SCALE` (1000); the server clamps the axes and the projection normalises, so magnitude never buys speed and nothing is trusted (invariant 3).
- **Sliding collision:** moving into a wall clamps flush; moving diagonally along one slides — the blocked axis stops, the free axis keeps going — so free movement never wedges on geometry. Collision is continuous (the footprint never enters an unwalkable tile), derived identically on every client and the server from the one shared projection.
- **Facing stays cardinal:** the synced standing facing (`player.faceX`/`faceY`) is the heading's dominant axis, so the tile mechanics that hang off facing — interact, attack, throw — keep their crisp grid targeting on top of free movement. `facingTile` treats a *mostly-cardinal* press (minor axis ≤ 0.35 of the major) as facing its dominant neighbour, so a slightly rotated camera doesn't break faced-tile targeting.

Both are **input-driven, not per-frame.** The client writes a movement or facing intent only on input transitions — a click (which sets a path), a key down/up, a direction change — never on a timer or every frame. Origins are **fractional**: an intent is sent from wherever the trogg stands, and the server settles the previous motion by deriving the same position from the same projection — nothing snaps, so there is no sub-tile distance to bank and a flurry of clicks or keypresses can't speed the trogg up (invariant 3). A held WASD run still re-bases its origin once per tile crossed (a motion transition, never a per-frame timer — invariant 2), so a trogg's position is only ever derived over the last tile rather than from a far-back run-start origin (see Obstacles for why). The server stores each player's motion as an origin `(x, y)`, a direction or a path, and `movedAt`; standing facing is stored separately as `faceX`/`faceY`. Position over time is derived from motion only, and clients render it locally between updates.

- Move speed: 4 tiles/sec *(initial)*, shared by both modes. A click overrides held keys and vice versa.
- **Running:** holding **shift** while moving runs at `RUN_SPEED_TILES_PER_SEC` (7 tiles/sec *(initial)*) instead of walking, controlled by the optional `running` flag (off → shift is ignored, movement stays at walk speed). Run state rides the synced motion intent (`player.running`), so `projectMotion` derives the same faster position on every client — no per-frame sync, no determinism mismatch (invariants 2 & 3). Releasing shift, stopping, or a change of direction re-bases the origin at the current speed (the `move` reducer settles before storing the new intent). Troggs show a faster, hunched run clip (the run gait in `src/game/rig.ts`); Hogs always walk.
- **Obstacles:** tiles carry a walkability flag; nodes and scenery (trees, rocks, walls) sit on unwalkable tiles. WASD clamps at the first unwalkable tile or the zone edge; click-to-move routes around them. Beyond the static tilemap, **dynamic obstacles** — boulders, trees, and Hogs — block a trogg the same way a wall does (see [Boulders and trees](#boulders-and-trees) and [Hogs](#hogs-roaming)). Walking into a Hog, a trogg **stops flush** against it — its motion settles to a standstill on the tile, rather than holding a "still walking" intent — and resumes the instant the Hog ambles off. This matters because position is *derived* from the intent over elapsed time: a held walking intent jammed behind a moving obstacle banks travel it never made, so when the obstacle clears the derivation would fling the trogg to where the uninterrupted walk would have reached (and the server, re-deriving the same way, would snap it there). Settling to a standstill on contact keeps the intent honest, so there's nothing to release; a click-to-move route that a Hog steps onto likewise stops the trogg flush rather than banking the rest of the path. The mirror hazard is a Hog wandering onto a tile the trogg has *already* crossed: re-deriving from a far-back origin against the now-occupied tile would clamp the trogg flush against it — visibly rewinding it backward. A held WASD run re-bases its origin once per tile crossed (see Movement, above), so an already-passed tile sits behind the origin and can't reach back to rewind it (the WASD counterpart of the forward-only click-to-move projection and the Hogs' tile-by-tile amble); any correction is bounded to the current tile.
- **Pathfinding:** the server runs grid `A*` over the zone's walkable tiles on each click, then **string-pulls** the route (`smoothPath`): from the fractional start it greedily keeps the furthest waypoint reachable in a straight line (line-of-sight tested with the same sliding-collision walker movement uses), so open floor collapses to one direct glide at any angle and only genuine corners keep bends. The smoothed `path` is stored on the player and clients animate along it — no client-side recompute, so no determinism mismatch to manage. A hop being traversed re-checks collision live, so a Hog landing mid-glide clamps the trogg (the stall the client re-routes on) instead of being walked through.
- **No teleport-by-quit.** The stored `(x, y)` is the *origin* of the current move, never the destination. Position is only advanced by elapsed real time × speed along `path`, so clicking far away and quitting skips nothing — on return you're wherever the clock puts you (arrived only if enough time actually passed). A scheduled task may settle `(x, y)` to the path's end at `movedAt + traveltime`, but never before it.
- **Prediction (display only):** because clients sync *intents*, not positions, every client derives the same motion locally and animates other troggs continuously from their last known intent — no per-frame position sync. Clients map the server's `movedAt` onto their local monotonic clock so deployed latency does not make avatars trail their authoritative motion by a round trip. The local player's own accepted input is applied optimistically to display state at the (fractional) spot where it was sent; a matching server row is treated as an acknowledgement and does not restart the animation, while a mismatching row still snaps to server truth (invariant 3).

### Boulders and trees

Boulders and trees are the world's **gathering obstacles**: solid like walls (walkability is the static tilemap **minus** the tiles they occupy), removed only by the matching tool. There is no pushing and no picking them up — a boulder is a mining node, a tree is a woodcutting node, and clearing one is how a blocked way opens.

- **Nodes have health and take swings** (`BOULDER_MAX_HEALTH` 45, `TREE_MAX_HEALTH` 54 *(initial)* — roughly three average hits each). Every tool swing that reaches its node rolls the tool's `WEAPON_DAMAGE` range into the node's health, and the client answers each hit with the same feedback a creature gives: a white hit flash and a floating damage number. The breaking hit deletes the row and **drops the yield on the floor** by the node (`dropLoot` — the node's own tile is free once it breaks), whatever weapon dealt it. Nothing enters an inventory uninvited: picking the yield up is a conscious `E`, same as any loot.
- **Any weapon works, badly** (`OFF_TOOL_NODE_FACTOR`, 8% *(initial)*): when a swing finds no creature and no matching node, it scratches the nearest node for a fraction of the roll (never below 1) — a sword can whittle down the first tree for the wood that crafts a real axe, it's just a terrible saw. Creatures always outrank off-tool node scratches in the same swing.
- **Mine a boulder with a pickaxe.** Pressing `F` with a pickaxe equipped strikes the nearest boulder in melee reach along the aim (the same reach + swing-cone geometry as combat); the breaking hit drops one **Stone** where the boulder stood.
- **Fell a tree with an axe.** The exact mirror: `F` with an axe equipped chops the nearest tree in reach; the felling hit drops one **Wood** where the tree stood. The axe shares the pickaxe's overhead `chop` wield class.
- Both start from the zone's `boulders`/`trees` registry entries, seeded idempotently on first connect, and are healed by `healStaleWorld` after a map regen like every seeded row. Trees seed on dry open floor only (`assertZones` checks it).
- **Resetting:** the Commands panel can snap the player's current zone back to its registry boulder layout (the `resetBoulders` reducer clears and reseeds the zone). Behind the `boulder-reset` flag — off, the panel hides the boulder reset action. The same panel resets Hogs (GDD "Hogs").

### Interacting (pick up and carry)

`E` is the generic **interact** key. It picks objects up from adjacent tiles and uses the faced tile first when several candidates are in reach, so later interactions (flip a switch, light a fire) can hang off the same key without adding a new control.

- **Pick up / put down is a toggle for common Hogs.** Empty-handed, pressing `E` beside a common (1×1) Hog **lifts it onto the trogg** — if several candidates are adjacent, the one on the tile the trogg faces wins. It leaves its tile and rides on your person, drawn as a full-size held overlay above the head (the same held-item layering as [Avatars and equipment](#avatars-and-equipment)); pickup changes its position, not its scale. Pressing `E` again **puts it down** on the faced tile (or the nearest free tile), re-materialising it in the world. A trogg carries at most one thing. Boulders are **not** liftable — they're mining nodes (see [Boulders and trees](#boulders-and-trees)) — though the put-down and throw paths still honour a legacy carried boulder on an old player row.
- **Throw what you carry.** Pressing `F` while carrying throws it in the faced cardinal direction as an instant, tile-based action. It travels up to `THROWN_OBJECT_RANGE` (4 tiles *(initial)*), stops at the first wall or solid object, and damages the first living trogg or Hog it hits by `THROWN_OBJECT_DAMAGE` (40 *(initial)*). The thrown object lands at the farthest clear tile, or near the target it hit.
- **Ground items are picked up by radius, not facing** (`ITEM_PICKUP_RADIUS`, 1.75 tiles *(initial)*, centre to centre): empty-handed `E` lifts the **nearest** ground item within reach into inventory — a drop at your feet is always liftable regardless of which way you look. Every ground item carries a **pickup sparkle** (gold motes circling above it), the universal "you can E this" signal — loot, gathered yields, and player drops alike. Starter pickups in `hog-town` are a pickaxe, shovel, axe, sword, shield, and torch. Items are not solid. Hog pickup keeps the adjacent scan with faced-tile priority.
- **Carried things leave the world.** A carried Hog stops wandering — its row is removed while held and re-inserted on drop. Hogs are fungible (no identity, seeded from the `ZONES` registry), but a carried Hog's skin is copied onto the player row and restored on put-down so pickup/drop never changes its look.
- **No tick, server-authoritative** (invariants 1 & 3). `interact` is an input-driven reducer. The client passes its current heading; the server re-derives the trogg's tile and acts only on adjacent entities or ground items, preferring the faced tile when there are multiple candidates, so the client can't reach past its neighbours. Carrying changes nothing per-frame — the held thing simply moves with its carrier, whose position is already derived.
- **Nothing is orphaned.** On disconnect the trogg drops what it holds where it settles; the carried kind is durable on the player row, so even a mid-carry restart loses nothing.
- Behind the optional `interact` flag (off → `E` does nothing).

### Hogs (roaming)

Ambient **Hog** NPCs (the glossary's friendly hedgehogs) roam the zone on their own — decorative life in the world, controlled by the optional `roaming-hogs` flag (off → no Hogs). This is presence and movement only: no dialogue, trade, or interaction. Merchant, townsfolk, and dialogue roles are separate later work.

- **Same motion model as troggs.** A Hog carries an intent — origin `(x, y)`, a cardinal direction, `movedAt` — and clients derive its position with `projectMotion`, so it's never per-frame synced (invariant 2).
- **Hogs are solid.** A Hog blocks troggs and blocks other Hogs — you can't walk through one, and two Hogs never share a tile. Troggs do **not** collide with each other. A Hog collides against walls, boulders, trees, troggs, and other Hogs; a trogg collides against walls, boulders, trees, and Hogs (the `occupied` predicate of `zoneBounds`, the one seam every blocker set builds on). A trogg that walks into a Hog **settles to a standstill** flush against it and resumes when the Hog moves off (see [Movement](#movement) → Obstacles), so a held direction against a roaming Hog never banks a stale step the server would later release as a teleport.
- **Variety and big hogs.** A common Hog's hedgehog skin is derived from its id (`COMMON_HOG_STYLES`: classic, snow, ember), so a zone reads as a varied crowd with no per-Hog field. A few **big Hogs** are placed per zone (the `bigHogs` registry list): a buff flexing hog and a dino-costume hog that occupy a **2×2 footprint** and render at double size. Their `style` is stored on the row (so they never roll from the random crowd), and the footprint span comes from `hogSize(style)` — the whole motion/collision stack (`projectMotion`, the wall clamp, `walkableCardinals`, the wander tick, and every blocker set) is footprint-aware off that one value, defaulting to 1×1. A big Hog blocks its full 2×2 and isn't liftable (the carry overlay is one tile). The chicken-costume hog is a built-but-unplaced **easter egg** (`HOG_STYLES` includes it; it's summoned, never seeded). All styles share the one rig and style registry (`shared/creatures.ts`).
- **Fluid 8-way runs, driven by a scheduled reducer, not a tick.** The scheduled `wanderHogs` reducer (SpacetimeDB's deterministic timer — the sanctioned exception in invariant 1, like respawns) still fires per tile-crossing time (`HOG_STEP_INTERVAL_MS`), but a Hog's run is now a **single gliding intent**: any of the 8 directions (`walkableSteps` — diagonals never squeeze wall corners), continuing write-free from its stored origin until the step ahead is blocked, another Hog has claimed it, or a `HOG_TURN_CHANCE` roll ends the run — then it settles to its tile and picks fresh (idle with `HOG_IDLE_CHANCE`). Each tick a moving Hog claims the footprint one step ahead, so two Hogs never take the same tile; a blocked Hog settles within one tick, so freed Hogs never bank more than a step of travel. Straight runs cost zero row updates — the fan-out cost of a hundred roaming Hogs drops to their turns. Randomness is the context RNG, so the schedule replays deterministically (invariant 3). (The `hog` row keeps its `path`/`homeX`/`homeY` columns — unused — so the schema stays put.)
- **Empty zone, no work.** The timer re-arms only while a player is online; when the last player leaves, the next tick settles every Hog to rest and stops, so an empty world produces no diffs and no work (invariant 1).
- **Server-owned, seeded from the registry.** Hogs have no identity. Their starting tiles come from the zone's `hogs` (common) and `bigHogs` (placed 2×2 showpieces) registry entries (`ZONES` in `shared`), seeded into the `hog` table on first connect (idempotent, like boulders), then moved only by `wanderHogs`. `assertZones` checks each big Hog's 2×2 footprint is clear floor. A Hog's `style` column is usually empty, meaning its skin/size is derived from id; Commands panel spawns store an explicit `HOG_STYLES` value so clicking a skin button creates that exact look.
- **Resetting:** the Commands panel can snap the player's current zone Hogs back to their registry population (the `resetHogs` reducer clears and reseeds the zone, the mirror of `resetBoulders`) — the cull for a zone overrun with extra panel-spawned Hogs (though `spawn` refuses past `MAX_HOGS_PER_ZONE` per zone, enforced server-side, so a zone can't be flooded to begin with). A Hog a trogg is carrying rides the player row, not the `hog` table, so it survives the cull and re-materialises on put-down. Behind the `hog-reset` flag — off, the panel hides the Hog reset action.

### Camera and rendering

- 3/4 top-down (RuneScape-2004 / Stardew view), now rendered as a real 3D scene: a fixed camera above and south of the zone, tilted down at the cave.
- Rendered with **Three.js** (WebGL) on a Vite + TypeScript client (`src/game/`). The world is low-poly flat-shaded 3D built entirely in code — no modelled assets, no editor files. Terrain reuses the procedurally painted cave textures (nearest-filtered so the floor stays chunky); walls are beveled blocks read from the same tilemap movement collides against; glowmoss tiles cast real teal point lights; a warm key light casts shadows and dark fog closes the cave in. The camera auto-fits the whole zone at the 3/4 angle on resize; click-to-move raycasts the pointer onto the floor plane to pick the tile. The client subscribes to the zone's SpacetimeDB tables and draws them; all authority stays server-side (invariant 3).
- **Draw budget.** The render loop caps at `FRAME_CAP_FPS` (60 *(initial)*) — ProMotion displays would otherwise run 120Hz for double the heat and no gameplay benefit. A zone's trees and boulders draw as a handful of whole-zone instanced meshes (`src/game/nodes.ts`, one draw per template part; the hit flash rides per-instance colour), only the nearest `CREATURE_BUDGET` creatures of each kind render as full jointed rigs, and identical shapes and immutable flat colours across every code-built model share pooled GPU geometries/materials (`src/game/pool.ts`; anything a model mutates per instance — player tint, hit flash, downed fade — stays per-instance).
- **The far crowd.** Creatures beyond the full-rig budget don't vanish — they render as species-shaped, species-coloured instanced silhouettes (`src/game/crowd.ts`, two draw calls for the whole distant population) that still glide and turn with their live projected motion, so a zoomed-out world reads inhabited. Hidden-tier creatures keep projecting, colliding, and making sound either way; corpses get no silhouette.
- Visible in-game HUD surfaces — chat history/input, the top-left icon toggle bar (Appearance `P`, Inventory `I`), and the **game menu** (`Esc`) — are HTML/CSS overlays above the canvas; the pre-alpha **Commands drawer** (`` ` ``) is deliberately on the opposite side — a lone toggle in the top-right corner opening a full-height right-edge drawer — so debug chrome never crowds player controls, so the browser owns layout, text input, focus, IME, and resize. They use `pointer-events` so a click on a panel is consumed by the DOM and a click on open space falls through to the canvas (click-to-move). The toggle bar is an accordion: opening one menu closes the others. **`Esc` is the single menu key** (`src/ui/menu.ts`): with something open it closes it (a drawer, the map, or the menu itself); on a clear screen it opens the game menu — a pause-style modal whose root is a short list (**Settings**, **Help**, and **Log out** when signed in); picking Settings or Help drills into that page with a Back control, and Escape steps back to the list before it closes. (The guest **Claim account with Discord** button lives under Appearance.) A focused chat/rename field owns `Esc` (blur), so it only reaches the menu when you aren't typing. World-space labels, health bars, and speech bubbles over troggs are camera-facing canvas sprites inside the 3D scene (`src/game/overlays.ts`), drawn depth-test-off so a wall never swallows a nameplate. The Help tab lists only enabled controls, and the Commands drawer exposes only enabled debug tools.
- **First-time onboarding (the coach, `src/ui/coach.ts`).** New players can't discover the verbs by clicking — pickup is `E`, equip is a right-click in the pack — so the first time a trogg reaches a milestone it raises one short hint card (bottom-centre, above chat), shown **once ever** (seen ids persist in `localStorage`; a returning player is never re-taught). Milestones announce via `coachHit(id)` (a window event, so no component holds a coach reference): waking in the birth cave (`find-pickaxe`), the first inventory gain (`first-pickup`), the first equip (`first-equip`), and first stone/wood (`mined-stone`/`chopped-wood`). Hints queue and show one at a time; click or wait to dismiss.

### Audio

- Sound effects are client-side feedback only; they never affect reducers, synced state, or analytics.
- The mix keeps footsteps under the theme: self and nearby strides sit low, giant 2×2 Hogs **thud** (slow, pitched down) where the small ones patter.
- **The game theme is composed in code** (`GameTheme` in `src/theme.ts`), like every other asset: a slow generative ambient — two detuned pads breathing a low drone under sparse pentatonic plucks with a feedback-delay tail — via WebAudio, started on the first user gesture. No music files, no licensing.
- **World sounds are distance-based** (`EARSHOT`, 14 tiles *(initial)*): nearby troggs' footsteps, Hogs' patter (the same strides pitched small and quick), and Hog snuffles play with linear falloff from the local trogg and are silent beyond earshot, as are boulder moves heard from afar. A world of a hundred Hogs never becomes a hundred footstep tracks.
- The current audio surface is event-driven: local trogg footsteps fire when movement starts and when the trogg crosses tiles; a moving boulder row (a throw landing) plays a settle hit and a felled tree plays the same crash; Hog heading changes may play a sparse, low-volume, distance-faded Hog snuffle after the initial subscription snapshot; chat, commands, command errors, and the ghost haunt play short UI/ghost cues; a live inventory gain plays a pickup cue voiced by material (stone rattles, wood knocks, anything else the soft item thump).
- **The player owns the mix.** Every sound in the game belongs to exactly one of four categories — **Music** (the theme), **Footsteps** (troggs and Hogs), **World** (boulders, Hog snuffles, the ghost), **Interface** (chat, commands, pickups, errors) — mapped per cue in `src/audio.ts` (`CUE_CATEGORY`) and enforced by the type of `src/sound-settings.ts`, so a new cue must pick a slider. The Settings tab of the game menu (`Esc`) holds a 0–100% slider per category; levels apply live (the theme re-mixes without restarting) and persist in `localStorage`, which the landing page theme honours too.
- Browsers can block playback before the first user gesture, so rejected play attempts are ignored and retried on the next cue.
- Source files and license notes live under `assets/audio/ATTRIBUTION.md`. Do not wire entire packs wholesale; pick short cues, keep volume conservative, and trim/normalize before promoting a candidate into `src/audio.ts`.

### Avatars and equipment

- A trogg (and a Hog) is a **jointed 3D model**: a low-poly flat-shaded body built in code on a shared skeleton, animated by real animation clips. Troggs and Hogs share the joint vocabulary, so equipment renders the same on either.
- **Creature rig (skeleton + clips).** Every creature is a hierarchy of named joint nodes — `Bob` (the gait dip), `LegL`/`LegR`, `Torso`, `ArmL`/`ArmR`, `Head`, plus the equip attach nodes `HandR` (main hand) and `HandL` (off hand) — built per species in `src/game/creatures.ts` over the shared machinery in `rig.ts`. Animation is **data played by the engine**: idle (breathing), walk, run (faster stride, forward hunch), and one attack per **wield class** (wind-up → strike → recovery, timed to the synced equipment-use impulse) are procedurally authored `AnimationClip`s played through Three's `AnimationMixer` with crossfades. Weapons declare their wield class on the item registry (`ITEMS[..].wield`): the sword **stabs** (a level thrust), the pickaxe **chops** (overhead slam), the shovel **scoops** (low dig and heave), and bare fists (or anything unclassed) **swing**. Each clip drives the arm, torso, *and the hand node as a wrist* that aims the held item's business end through the arc — authored once per class in `rig.ts`, parameterized by the species' `GaitSpec`, so every creature on the joint vocabulary performs every weapon correctly. Gaits are layered — a legs layer (legs + bob) under an upper-body layer (arms + torso) — and an attack replaces only the upper layer, so attacking on the move never stops the feet. There are no per-facing frames — the model yaws smoothly toward its heading; the cardinal `Facing` survives as game logic (movement intents, interact targeting). Equipment use plays the attack clip, so the creature's **arm actually swings**; a held item is parented to the hand node, so it rides the swinging arm with no placement math. Taking damage plays a **hit flinch** — a short recoil opposite the facing plus a solid white flash (materials driven by `flash`), triggered when a creature's health drops; troggs and Hogs both flinch.
- **Held items** (pickaxe, shovel, axe, sword, shield, torch) render as models parented to the rig's hand nodes — a **main hand** (`HandR`) and an **off hand** (`HandL`), so combinations like sword + shield or sword + torch work. Live held-item behavior — a light, animated flame cels, a holding-arm pose — is authored **once per item** as its `HeldFx` (`src/game/items.ts`), consumed verbatim by both the game and the `/preview` page, so a new weapon never redefines how it is held per surface. An equipped **torch** carries a pool of warm firelight — the landing page's torch made portable: hard-swapped sculpted flame cels, an off-hand arm posed aloft after each mixer update, and a point light at the flame flickering on offset sines; torch lights have their own budget — only the nearest `TORCH_LIGHT_BUDGET` visible torch-bearers shed real light, the glowmoss budget's reasoning — so a torchlit crowd never multiplies the forward renderer's per-fragment light cost. A new holdable is one small model builder in `src/game/items.ts` (grip at the origin, business end along +y), not a new character: parenting to the hand makes it swing through the walk/run cycle and lead the strike on attack for every species at once. Occlusion is free — the depth buffer handles it. Items rest **perpendicular to the forearm** — the way a fist grips a haft — tuned by a per-item rest pitch (sword up at the ready, pickaxe hafted forward, shovel low); the shield alone straps along the forearm, face out, raised into guard. Each species declares an item **fit scale** on its model (`finishCreature`), so a hog grips hog-sized gear — the minimal form of the per-(item, species) fit in the layered-equipment design below. The inventory, equipped-slot, and spawn-button icons render from these same models (`src/game/icons.ts`), so every surface shows the exact object the world renders; the HUD panel toggles (inventory/commands/appearance/help) render from tiny low-poly props through the same rig, so no icon in the HUD is flat art.
- **Armor (later)** attaches the same way to body joints (head, torso). The joint vocabulary reserves the anchors now; armor models are added with the mechanic.
- What's equipped rides the zone's player sync, so others see what you're holding. Pressing `F` uses what the trogg is currently holding or wielding without stopping movement — and holding `F` keeps using it at the swing cadence, so chained attacks are a hold, not a tap per swing: a carried boulder or Hog is thrown, a pickaxe mines the nearest boulder in reach of the swing into Stone, a sword attacks the nearest creature in reach (GDD [Combat](#combat)), and a shovel currently shows its synced use animation only. The tools, the stone resource, and the boulder are low-poly props in the same code-built style as the creatures (`src/game/items.ts`); the world palette lives in `src/game/palette.ts`.
- **Model avatars:** a trogg renders as its jointed 3D model (styles `moss`/`stone`/`ridge` vary palette and brow), feet anchored at the centre of its tile. The per-trogg colour multiplies the body's materials, so the same trogg is the same colour for everyone, every session; your own trogg gets a ground ring so you can pick it out. (The retired `avatar-sprites` flag is noted in [analytics.md](analytics.md).) The dev `/preview` page inspects every creature model, clip, and held item, URL-addressable for shareable deep links; concept references live in `docs/art-refs/`.
- **Hog ball form:** each common Hog style has a defensive **ball** model — the hog curled spiky-side-out (`buildHogBall`, `src/game/creatures.ts`), one pose, no rig (a ball reads the same from any side). **A picked-up Hog renders as its ball** — the carry overlay swaps the upright model for the ball, so lifting a Hog curls it. The chicken easter egg has no ball and rides upright; big Hogs (buff/dino) aren't liftable, so they never need one. The further curl-when-attacked-then-roll-away behaviour is later work.
- **Two appearance axes — style and colour.** A trogg's look is a **body style** (the model's shape and base palette) plus a **colour tint** over it; the two are independent, so any style can wear any colour. Each is the value the trogg chose, or — until it chooses — a stable default derived from its durable id (a deterministic projection, like a level from XP; `STYLE_UNSET` / `COLOR_UNSET` = -1 are the unchosen sentinels). Both ride the zone player sync, so the model (and the trogg's chat-name colour) update everywhere they're shown.
- **Trogg style (`trogg-restyle`):** styles come from a fixed list (`TROGG_STYLES` in `shared`: `moss`, `stone`, `ridge`) that vary the silhouette features (ear nubs / earless crag / horns) and base palette — same rig, different head and tone. A trogg picks one via the `restyle` reducer, which stores its chosen index on the `player` row (validated server-side, invariant 3). The optional flag controls whether the style buttons show in the Appearance panel.
- **Trogg colour (`trogg-recolor`):** the tint comes from a fixed palette (`TROGG_COLORS` in `shared`), chosen via the `recolor` reducer (the mirror of `restyle` on the colour axis). The optional flag controls whether the palette swatches show in the Appearance panel.
- **Hog variation.** Ambient seeded Hogs usually leave `hog.style` empty, so each skin is derived from entity id (`COMMON_HOG_STYLES`: `classic`, `snow`, `ember`), giving a zone a varied, stable crowd. A concrete `hog.style` pins an exact look: the `BIG_HOG_STYLES` showpieces (`buff`, `dino`) render at double size over a 2×2 footprint (`hogSize`), and `chicken` is a built-but-unseeded easter egg — the full set is `HOG_STYLES` (`classic`, `snow`, `ember`, `buff`, `dino`, `chicken`). Commands panel spawns store the selected style so the look is exact. Once a Hog is picked up, its effective style is stored as `player.carryingStyle` and written back to `hog.style` on put-down, so the replacement row keeps the same look despite getting a new id. Hogs are never tinted.
- **Ghost (`ghost-trogg`):** the cosmetic easter egg is its own bespoke model (`buildGhost` — a translucent draped sheet, two eye holes, stub feet), never tinted. Summoning it inserts a zone-scoped `ghost_haunt` row; live clients in that zone render the fresh insert once as a slow materialise, gentle drift, linger, and fade-out, while late joiners ignore the replayed snapshot.
- **Appearance panel.** Rename, recolour, and restyle are one top-left HUD icon toggle (`P`, beside Inventory) — everything about how your trogg *looks* in one place. Sign-in/log-out lives in the game menu (`Esc`), gated by `auth-enabled`.

#### Layered avatars and cross-species equipment (design)

The target the rig is being built toward: a creature is not one model but a **stack of attachments** — base body, eyes, hair, outfit, armour pieces (breastplate, helmet, …), and held items — parented to its joints. Cosmetics and equipment are attachments you add, so players look different and gear is visibly worn. This is the paper-doll / layered-equipment pattern, and it's cheap here: an attachment is a model parented to a named joint, it animates with the body for free, and the depth buffer does the occlusion.

- **The rig is a cross-species slot contract.** It generalises from today's hand nodes (`HandR`/`HandL`) to a fixed vocabulary of named **slots** — `head`, `eyes`, `face`, `chest`, `mainHand`, `offHand`, `back`, `feet` — each an attach node (a transform, plus an optional fit scale) defined **per species**, once — the pose comes from the skeleton, not per-facing tables. The slot *names and semantics are shared across species*, so any attachment that targets a slot can be placed on any creature that implements the rig. A new monster race = build its body on the joint vocabulary; it then supports every existing item.
- **Item identity is species-agnostic; item *rendering* resolves per (item, species).** An item (a breastplate) is one registry entry — id, slot, stats, drop, equippable — independent of who wears it. How it *looks worn* is resolved per wearer: a **bespoke per-species model variant** when authored, else the shared model scaled by that species' slot fit, so a new species wears every existing item on day one (polished with bespoke models later). Stats travel with the item; fit travels with the wearer.
- **Runtime attachment, synced loadouts.** The look of a creature is its equipped + cosmetic set. Each player syncs its slot choices (extending today's `equippedMainHand` and the appearance style/colour axes); every client parents the loadout's models to the wearer's joints — no texture baking, no combination-space caching. Per-attachment material tint is how colour variety (hair, armour) rides without separate models per colour.
- **Cross-species lifecycle.** A monster dies → drops the item as a species-agnostic `ground_item` → any creature picks it up and equips it → it renders with that wearer's own fit. The drop, the inventory row, and the stats are the item's; the silhouette is the wearer's.

Current state this generalises from: **both hands are equippable and synced gameplay-side** — `equippedMainHand` and `equippedOffHand` (each with its inventory-row pointer) route by the item's slot, a **shield** equips to the off hand from the inventory panel, and every client parents both item models to the wearer's hand nodes (the shield is cosmetic for now — it has no block effect yet). Every creature but nothing-special cases implements the full joint vocabulary, so any slot-targeted attachment can land on any of them. What's *not* wired yet is the wider slot vocabulary (head/chest/…), which lands with its first consumer. Attachments are then added cheapest-first (eyes/hair before armour).

Open questions to resolve before building: whether a slot carries a fit scale beyond the species-wide one shipped today, or a full transform; per-attachment tint vs separate models for colour variety; and how cosmetic-only attachments (hair, eyes) fold into the existing appearance/`restyle` model.

### Zones

- A zone has a slug, display name, integer width/height in tiles, and a tilemap: per-tile walkability plus scenery. Nodes and obstacles sit on unwalkable tiles.
- **Tile glyphs.** Each character of a zone's `tiles` rows is a tile glyph (`TILE_GLYPHS` in `shared`). `WALL_TILE` (`#`) is the only unwalkable glyph — `isWalkable` keys solely off `#`, so collision and pathfinding ignore the rest. The other glyphs are **cosmetic floor variants** that dress the stone so a zone reads as varied terrain instead of one flat fill: `.` plain floor, `,` gravel scree, `"` moss, `~` shallow water (walkable — a puddle the trogg wades through; an impassable pool would be a `#`-class glyph), `*` glowmoss. The client lays a tile-sized decal quad per variant (`src/game/terrain.ts`); they change rendering only, never movement (glowmoss also casts a cosmetic point light). `assertZones` rejects any glyph not in `TILE_GLYPHS`. Add a new variant by registering its glyph and an overlay painter together.
- **Zone definitions are a static code registry** (`ZONES` in `shared`, keyed by slug) — static design data like the item and node registries, not the durable `zones` table the data model lists. A zone's tilemap may be **procedurally generated**: `generateCaveZone` (`shared/worldgen.ts`) grows a cave from a fixed seed with pure deterministic arithmetic (mulberry32, no `Math.random`), so the client and the module derive the identical world — generation guarantees a solid rim, an open spawn plaza at the centre (`spawnAt`), full reachability from spawn (disconnected pockets are filled), and seeds (boulders, Hogs, starter tools, giants) on open floor, all covered by tests. Change the seed or dimensions in the registry entry to reroll the world. The table is deferred until tilemaps need editable storage; until then a zone is a registry entry. A client subscribes to one zone's rows by slug (`WHERE zone_id = …`); zone dimensions and the tilemap (`tiles`, walkability read through `isWalkable`) come from the shared `ZONES` registry (imported by both the client and the module), so the grid is shared design data, not a per-session value. The client renders walls and floor variants from the same tilemap it collides against, so what's drawn is what blocks you.
- **Spawn and the hub gate:** new players spawn in a shared **starting zone** (working name: the cave) and must reach a checkpoint to unlock the hub, `hog-town`. The hub isn't available until the checkpoint is crossed — a per-player progression gate, not an instance; the starting zone is shared like any other.
- **The world is ONE seamless zone** (slug `world`): a 224×208 **continent**, grown like terrain rather than assembled from blocks — an irregular landmass (radial falloff + value noise), eleven **organic regions** shaped by warped Voronoi around hand-laid capitals (`WORLD_REGIONS` in `shared/worldgen.ts`), and ONE global cave automaton whose rock density varies per region so borders blend with no seams. Two **rivers** of deep water (`=`, unwalkable — the second `SOLID_GLYPHS` entry) wind across the continent, crossable at recurring shallow **fords**. Impassability reads in the **z-axis**: walls and boulders rise, rivers sink — deep water is cut out of the floor and rendered as a recessed channel with dark banks, so terrain you can't cross never looks like floor. **Terrain placement rules** keep the world sensible: seeds, drops, and Commands-panel spawns land only on *dry* open floor (`isDryFloor` — never in rivers, fords, or pools), and Hogs keep to the banks (water blocks their wander like a boulder does). A trogg still wades through shallow water; **Hog Town** is a real town at the spawn — a gravel plaza ringed by stone huts with doorways facing in. Regions are colour/decoration character and a name for a part of the map, not instances; the per-tile region grid is committed alongside the tilemap and read by `regionAt`.
- **The world heals under regeneration.** Rerolling the committed map under a live database strands old rows: `healStaleWorld` (on connect) detects any boulder/Hog/ground item on unwalkable ground and wipes + reseeds the zone from the current map, and a returning trogg stranded in new rock or water relocates to the **nearest safe dry tile** beside where it logged out (`nearestSafeTile`), falling back to spawn only when nothing nearby is open.
- **The map is committed data.** `bin/generate-world` runs the deterministic generator once and writes `shared/world-map.ts` — the checked-in file is the truth both the client and the module read, so the world is identical for every player *and hand-editable*: touch tiles or seeds directly, and the worldgen tests (global reachability from spawn, seeds on open floor, per-region coverage) validate the edit. Rerunning the script rerolls the whole file.
- **The overworld map (`M`)** paints the committed tilemap downsampled 2×2 and tinted per region palette (`src/ui/worldmap.ts`) — sparser than the world but derived from the same data, so it is always consistent with what's actually there. It shows region names and the trogg's own live position (never other players); `M`/`Esc`/click closes it.
- **The game opens in third person, behind the trogg** — a chase view at shoulder height, not top-down, so the world reads at its own scale from the first frame; the orbit camera takes over from there (drag to look around). There is no first-person mode. An orbit camera can only look *at* its target, so on its own it bottoms out at the trogg's shoulders: instead, dragging down past the floor parks the camera skimming the ground and turns the extra drag into an upward pitch (`src/game/controls.ts`) — keep dragging and you look up at the sky, the sun, and the peaks; drag back and it returns the same way. **The trogg is never eclipsed by terrain**: each frame the client marches the trogg→camera sight line through the shared rock-height model and, if rock crosses it, the render camera rises on its orbit sphere — same radius and azimuth, the smallest pitch lift that looks over the blocker — briskly eased up, gently eased down, with a short hold before easing down so corners grazing the line while pathing can't pump the camera. The player's zoom is never touched, and it's render-only: the orbit keeps the pose the player chose.
- **A shared day–night cycle** (12 min *(initial)*, wall-clock phased so every player sees the same time of day): the sun arcs east→west with the shadows tracking it, dusk fades the sky and haze, and night falls to a dim moonlit blue where the glowmoss carries the light.
- **Daylight and scale.** The continent lives under a **sun**: a sky backdrop with aerial haze, a bright warm directional sun (riding the camera focus with its shadow box) and a cool sky bounce — a world, not a cave. Rock height is **terrain-shaped**: a distance field from the walkable floor keeps rock beside paths at shoulder height and raises formation cores into **mountains** (up to ~9 tiles), while low-frequency noise makes ranges undulate and throws occasional summits — deterministic, so every client carves the same skyline. Low camera angles read as standing in a valley among peaks, and the sun rakes long shadows off them.
- **The client streams the world in proximity chunks** (`src/game/terrain.ts`): 32-tile chunks build nearest-first (throttled per frame) around the camera focus and dispose with hysteresis — Google-Maps style. Each chunk is one floor texture (tiles blitted from the biome patch painters, deep water included), one instanced wall mesh (per-instance biome colours), and glowmoss lights that spawn dark under a global budget (the nearest 8 within 48 tiles are live — a forward renderer pays every light on every fragment). Camera zoom is **capped** (`maxDistance` 34) with only a faint fixed depth haze — no fog of war; the M map is the wide view. Distant creatures/boulders/items hide beyond `CULL_RANGE`, and creatures additionally render under a **visibility budget** (the light budget's pattern): only the nearest `CREATURE_BUDGET` troggs — and separately Hogs — draw at once, since each jointed rig is ~20 draw calls plus overlays and a shadow pass; hidden creatures still project, collide, and make sound, and their animation mixers pause. The shadowing key light rides the camera focus.
- Clients subscribe to players/nodes/chat **in their current zone only**. Within a zone, sync is deliberately naive (whole-zone queries) — see invariant 10.
- New zones ship incrementally; transitions are walk-to-edge or interact-with-passage. How later areas gate (checkpoints, light, communal projects) is an open thread.

### Chat

- Zone-scoped. Max 200 chars *(initial)*. Bubble displays 5s *(initial)*; side panel keeps recent history.
- Chat history renders as free-floating lines, each on its own translucent backing sized to the text — an empty chat shows nothing at all (no panel); the typing field is a real `<input>`; speech bubbles over troggs render as sprites in the 3D world. Chat content is added as a DOM text node, never as HTML markup, so a message can't inject markup.
- Server-side rate limit: 1 message/sec per player *(initial)*.
- Message content is **never** sent to analytics.
- **Pre-alpha Commands panel:** debug tools live in the top-left Commands panel — including a client-side **Hitboxes** toggle (`B`) that shows every creature's, boulder's, and tree's combat hit circle plus the local trogg's melee reach ring and swing cone (the cone follows the live aim and brightens while a use is in flight), not chat slash commands. The spawn grid uses icons rendered from the actual world models: one boulder button, one tree button, one button per `HOG_STYLES` skin/size, and one button per `SPAWNABLE_ITEM_IDS` item — the equipment (`pickaxe`, `shovel`, `axe`, `sword`, `shield`, `torch`) plus `stone` and `wood`, which are spawnable for material testing even though normal play gathers them through mining and woodcutting. Each click requests exactly one spawn; if someone wants ten Hogs, they press a Hog button ten times. The `spawn` reducer places the thing on a nearby free tile around the caller, starting with the tile they face, and clamps inserts to `MAX_BOULDERS_PER_ZONE`, `MAX_TREES_PER_ZONE`, `MAX_HOGS_PER_ZONE`, `MAX_GROUND_ITEMS_PER_ZONE`, and available floor so the UI can be abused without bypassing caps.
- **Ghost:** the Commands panel can summon the cosmetic ghost (a draped-sheet apparition; see [Avatars](#avatars-and-equipment)) at a server-picked random walkable tile in the caller's zone (behind `ghost-trogg`, fallback on). It fades in, drifts gently, lingers for a few seconds, and fades out. It inserts a capped `ghost_haunt` row so every live player in the map sees the same haunt; the row has no collision or durable gameplay effect, and late joiners ignore old rows.

### Identity

- Anonymous-first: SpacetimeDB issues each connection a cryptographic **Identity**; the browser stores the connection token it returns. Identity is the connection's own `ctx.sender` server-side, never client-asserted (invariant 3). Guests get a generated name `trogg-####` and exist within seconds, no signup.
- **Guest persistence:** the browser securely stores the SpacetimeDB connection token — not game state, which stays server-authoritative (invariant 3) — so a returning visitor reconnects with the same Identity and resumes the same trogg row with their progress intact. Clearing the browser or switching devices makes a guest a new trogg.
- **Surviving a redeploy:** the frontend and backend ship *independently* — the client to Cloudflare, the SpacetimeDB module to the VPS — so each deploy is handled on its own signal.
  - *Backend deploy:* SpacetimeDB is both the store and the live feed, so publishing a new module version closes every live socket at once. The client treats this as recoverable, not fatal: it shows a "reconnecting" overlay and probes for the server with exponential backoff + full jitter (jitter so the whole world doesn't stampede the instance the moment it restarts), then reloads once it's back. Because state is server-authoritative and re-derived from subscriptions, and the stored token resumes the same trogg, this loses nothing — players stay in the world across a deploy instead of being silently logged out. The reload also pulls the latest frontend as a side effect. Emits `connection_lost` (analytics.md).
  - *Frontend deploy:* a client-only deploy leaves the socket untouched, so there's no disconnect to react to. Each build is stamped (`__BUILD_ID__` + a shipped `version.json`); the client polls that stamp (every 60s and on tab focus) and, when a newer build is live, shows a dismissible "new version — refresh" prompt rather than forcing a reload — the old client keeps working against the unchanged backend, so the player refreshes when it suits them. Emits `client_update_available` (analytics.md).
- **Signing in** upgrades a guest to an account via **SpacetimeAuth** (SpacetimeDB's managed OIDC provider; Discord is the enabled login). SpacetimeDB derives a *stable* Identity from the OIDC token's `iss`+`sub`, so the account — not the browser — now anchors the synced state and the trogg resumes on any device. The browser runs the OIDC Authorization-Code-**+-PKCE** flow (a public client: no client secret in the bundle, invariant 8); the module trusts only the SpacetimeAuth issuer as an account provider (invariant 3). Account creation and the upgrade fire `player_named` alongside `posthog.identify()`, merging the guest's history.
- **Multiple live sessions for one account:** two tabs or devices signed into the same account share the same `player` row and therefore the same trogg. Any live session can send input; the other sessions simply observe the shared motion intent and render the trogg following it. Presence is tracked per socket in the private `player_connection` table, so opening a second session does not restart the trogg's movement, and closing one session does not mark the shared trogg offline while another session remains connected.
- **Claiming** (folding a guest's trogg into the account, since the two are different Identities): the guest's browser mints a one-time nonce, registers it under the guest Identity via `startClaim`, then signs in and redeems it as the account via `redeemClaim` — both sides proven, never a client-asserted identity (invariant 3). The guest's chosen name carries over (a generated `trogg-####` never overwrites a name the account already chose); the guest row is then absorbed. A fresh device with no guest just signs in and resumes the account directly. Nonces expire after `CLAIM_CODE_TTL_MS`.
- **Changing your name:** the `rename` reducer swaps the generated `trogg-####` for a chosen one, validated server-side. Names: unique, 3–20 chars, alphanumeric + hyphen. The new name takes effect everywhere it's shown — the nameplate over the trogg and the denormalised `name` on the player's past `chat_message` rows are both rewritten, so nothing keeps showing the old name.
- The account action lives in the game menu (`Esc`) — Sign in with Discord for a guest, Log out when signed in (log out drops back to the landing page). Rename, colour, and style live in the Appearance panel. Sign-in uses the browser OIDC redirect.

### Skills and XP

- Skills exist per player, per skill, as accumulated XP. Level is derived, never stored.
- Total XP to reach level L: `50 × (L − 1)²` *(initial)* — level 2 at 50 XP, level 10 at 4,050.
- Level cap: 50 *(initial)*.
- First skills: **mining** and **foraging**. Crafting can arrive when recipes and inventory need it. No combat skills until defense events are specced.

### Gathering (nodes and actions)

- Clicking a node routes the trogg to the nearest reachable tile adjacent to it (nodes are unwalkable). On arrival the action starts: the server validates (adjacent, node available, no action in progress), writes the action with an end time, and schedules its completion server-side (`endsAt` persisted so it survives a restart).
- Completion grants the item + XP, flips the node to `depleted`, schedules its respawn. If the player walked away mid-action, the action cancels.
- One action at a time per player. Starting a new one cancels the old.

| Node type | Skill | Action time | XP | Item | Respawn |
| --------- | ----- | ----------- | -- | ---- | ------- |
| stone | mining | 3s *(initial)* | 10 *(initial)* | Stone | 30s *(initial)* |
| glowcap | foraging | 3s *(initial)* | 10 *(initial)* | Glowcap Mushroom | 30s *(initial)* |

New node types are added by extending this table — keep it the registry.

### Inventory

- The top-left Inventory icon (`I`) opens the inventory/equipment panel. Starter pickup items are seeded from the zone registry as `ground_item` rows; pressing `E` while facing one moves it into the player's inventory and removes the ground row.
- Inventory has 20 visible carry slots *(initial)*. The HUD always shows all slot outlines, filled first by owned inventory rows and then empty slots, so players can see remaining capacity at a glance.
- Inventory rows store an item id and quantity, and each row occupies one visible carry slot. Stackable items (Stone, Wood) merge into one row and can grow without consuming more slots; a stackable item with no existing stack needs one free slot to start. Equippable items (Pickaxe, Shovel, Axe, Sword, Shield) are non-stackable qty=1 rows, so two swords remain two visible slots and the equipped row is unambiguous.
- Items are defined in a static registry (id, name, stackable, blurb). Holdable/wearable items also carry their slot and model. No item randomization.
- **Pickups announce themselves**: a live inventory gain (new row or a stack growing — never the boot sync) raises a bottom-centre toast with the item's icon and name; rapid gains of the same item bump one card's count instead of stacking duplicates (`src/ui/toasts.ts`).
- **Hovering (or focusing) any occupied tile — carry slots and both hands — shows a tooltip** with the item's name and registry blurb, so what a thing is for is discoverable in place. The same shared tooltip (`src/ui/tooltip.ts`) covers every HUD control (the top-left toggles, the Debug Tools toggle, the spawn tiles), always clamped inside the viewport. The page behaves like a game surface, not a document: text never highlights (real inputs excepted) and the browser's own context menu is suppressed — right-click belongs to the game.
- Equipping sets the slot the item belongs to — `equippedMainHand`/`equippedMainHandInventoryId` for tools and weapons, `equippedOffHand`/`equippedOffHandInventoryId` for shields — to an owned row (the item stays counted in inventory; equipment just references it). The reducer routes by the item's `slot`, so the two hands equip independently; equipping the row already in its slot toggles it off, and `0` clears both hands. See [Avatars and equipment](#avatars-and-equipment).
- **Right-click is the item verb**: a tile's context menu opens at the cursor — equip/unequip (equippables only, into the item's hand), **drop**, and **delete** (the entry arms into an in-place confirm; the second click destroys). Left-clicking a tile does nothing. Both drop and delete act on one unit — a stack decrements, a qty=1 row is removed; removing the equipped row unequips it. Drop lays that unit back into the world as a `ground_item` on the faced tile, the nearest free neighbour, or the trogg's own tile (the carried put-down placement), honouring `MAX_GROUND_ITEMS_PER_ZONE`; a full or boxed-in zone refuses the drop and keeps the item. Delete destroys the unit outright with no ground item, behind a confirm step in the panel.

### Combat

Pre-alpha combat is deliberately small and tile-based. Troggs and roaming Hogs are the current damageable characters; future PvE defense events use the same slow, stat-driven grammar rather than twitch mechanics.

- **Health:** each trogg has `PLAYER_MAX_HEALTH` (100 *(initial)*) health points. Health rides the `player` row, so everyone sees the same health bar and death state from server truth.
- **Hog health:** a roaming Hog carries `hogMaxHealth(style)` health on its row — `HOG_MAX_HEALTH` (60 *(initial)*) for a common, scaled by footprint area for giants (a 2×2 is four commons' worth). Damaged Hogs show a small health bar. At zero health the Hog becomes a **corpse**: settled where it fell, stopped, lying flat in the dead stance — not solid, not targetable, not liftable — until the regen sweep reaps it after `NPC_CORPSE_MS` (30s *(initial)*). The killing blow also **drops the creature's loot** (`hogLoot`: 1–2 Quills for a common, 4–6 for a giant *(initial)*, rolled with the context RNG) as ground items on the nearest free tiles around the corpse — the corpse isn't solid, so its own tile counts — honouring the per-zone ground-item cap. Troggs need no loot table: death already drops everything they carried and held.
- **Weapon attacks:** pressing `F` with a main-hand weapon equipped swings along the trogg's exact aim vector and damages the nearest online, living trogg — a trogg outranks a Hog at equal distance — whose **hit circle** is in reach: within `MELEE_RANGE_TILES` (1.7 *(initial)*, centre to circle edge) and inside the ±55° swing cone (`MELEE_ARC_RAD`; point-blank inside `MELEE_POINT_BLANK_TILES` waives the angle, and a target's radius widens the cone by what its silhouette subtends — `shared/combat.ts`). Troggs have a 0.45-tile radius, Hogs 0.42 scaled by `hogSize`, so a giant is as hittable as it looks. The client passes its aim, but the server re-derives every position with the same projections the client renders (invariant 3).
- **Bare fists always work** (`UNARMED_DAMAGE`, 5–10 *(initial)*): an empty-handed `F` swings too — the weakest weapon, stored as the `"fists"` action impulse so every client animates the bare swing. Fists follow the same swing resolution as weapons, off-tool node scratches included.
- **Out-of-combat regeneration**: a creature untouched for `HEALTH_REGEN_DELAY_MS` (30s *(initial)*) heals `HEALTH_REGEN_FRACTION` (5%) of its max, rounded up, every `HEALTH_REGEN_TICK_MS` (3s) — the scheduled `regenCreatures` sweep, the same sanctioned timer exception as `wanderHogs`: re-armed only while a player is online, writing only rows that actually heal. `lastDamagedAt` on the player and hog rows is the out-of-combat clock. Dead troggs never regen — respawn restores them.
- **Hog health scales with size** (`hogMaxHealth`): a 2×2 giant carries four commons' worth (240 vs 60 *(initial)*) — as tough as it looks.
- **Every weapon rolls its own damage range** (`WEAPON_DAMAGE` *(initial)*: sword 20–30, axe 14–22, pickaxe 11–19, shovel 8–16, inclusive). An accepted hit rolls inside the weapon's range with the reducer's context RNG, so no two swings land identically while replay stays deterministic. Gathering tools resolve their node first — a pickaxe swing that reaches a boulder mines it and spares the Hog behind it; an axe fells the tree — and wound creatures only when no node claims the swing. Off-hand items (shield, torch) and bare fists deal nothing.
- **Floating damage numbers:** every health drop a client sees (weapon hits and thrown objects alike) pops a rising, fading damage number over the target — anchored to the world, not the target, so a killing blow's number outlives its victim. Client-side display only, derived from synced health deltas.
- **Thrown Hogs:** pressing `F` while carrying throws the held object along the **exact aim** (free-direction, not the four cardinals; `throwCarried` marches the aim ray tile by tile) up to `THROWN_OBJECT_RANGE` (4 tiles *(initial)*). It damages the first online, living trogg or Hog it hits by `THROWN_OBJECT_DAMAGE` (40 *(initial)*) and then lands near the impact; if it hits no damageable character, it lands on the last clear tile the ray reaches before range or a blocker. This is still an input-driven reducer, not a projectile simulation — the server resolves the landing at once. A **thrown Hog then sits still for a fixed settle** (`THROWN_HOG_SETTLE_MS`, 900 ms *(initial)*): the wander skips any Hog whose `landingAt` is still in the future, so it can't stride off before it has visibly landed. The settle is a fixed window, not tied to the throw distance, so the server and the client's arc don't have to know each other's timing (the settle just outlasts the longest arc). The **client animates the flight**: the object arcs (a tumbling parabola) from the thrower's hands to the landing spot, and the real row is held out of the scene until it touches down (`world.ts`, cosmetic). The arc shows for everyone — the decision waits a microtask so the release (the thrower's `F`-press, or a bystander's carry-cleared update from the same transaction) is recorded first, then pairs with the object's insert.
- **Use cooldown:** the server drops an equipment use that lands inside the previous swing (`EQUIPMENT_USE_COOLDOWN_MS`, 250 ms *(initial)*, just under the 300 ms `EQUIPMENT_ACTION_MS` impulse), so attack rate is capped by the animation cadence, not by how fast a client can call the reducer (invariant 3).
- **No twitch checks:** combat has no projectiles, physics, cursor aiming, or per-frame attack checks — a use is one input-driven reducer resolving static hit circles at that instant.
- **Death:** when damage takes a trogg to zero health, the server marks it `dead`, stops its motion on its current tile, and leaves it online so other players can see it — lying flat in the dead stance (every creature shares it), faded translucent, under its respawn countdown. Dead troggs cannot move, interact, spawn objects, or use equipment. If a dying trogg is carrying a tile-sized object, the server tries to drop it at the death tile so nothing is orphaned. Every inventory row, including equipped items, is removed from the trogg and dropped as `ground_item` rows nearby; stack quantities are preserved.
- **Respawn:** death stamps `respawnAt` five seconds in the future (`PLAYER_RESPAWN_MS`, 5000 *(initial)*) and inserts a one-shot `player_respawn` scheduled row. Dead troggs show a visible respawn countdown. When the timer fires, the same `player` row returns at full health with the same name, colour, and style, **just outside your cave** — the world's coast alcove (`EMERGE_ARRIVAL`), where you first emerged and where you descend back down. A trogg that died inside its own birth cave is pulled out to the alcove too (the cave is for births, not a spawn room), which the client sees as a zone transfer.

### Onboarding: the Warren

Every trogg is born in the dark — **alone**. Onboarding is single-player by construction: each newborn gets a private copy of the shared `birthcave` template (**The Old Dark**, `generateBirthCave` — a small, fully enclosed cave), scoped by its own zone id `birth:<identity hex>` (`BIRTH_ZONE_PREFIX`). Zone-filtered subscriptions mean nobody else ever sees or reaches into it, so nothing can be stolen (the pickaxe grief is impossible) and no occupancy/reclaim machinery exists at all.

The arc: wake in the **middle of the glowmoss cavern** (a hearth ember lights the wake-up spot; the opening camera is raised and close), pick up the pickaxe beside you (`E`), equip (`I`), mine through the **rubble plug** — two rows spanning the cavern end of a long throat (`F`) — gather the stones the rocks drop, and **walk the throat out — the walk is the door** (the transfer threshold sits at its far end, so nothing whisks you away before you've picked up your haul). The throat **tapers as it climbs** — three wide leaving the cavern, then two, then one at the exit — and the cave never shows the outside: **glowmoss pools spaced along the walk** (template-stamped; the client lights each one, the sealed exit landing brightest) lead toward the end without ever revealing it. The `emerge` reducer verifies proximity server-side (invariant 3) and transfers the trogg to `world` at `EMERGE_ARRIVAL`, a cave-mouth alcove carved into the south coast. The client reboots into the new zone behind the boot screen; the arrival opens on a raised reveal shot, fires `warren_emerged`, and plays the **first dawn** (~12s local-only blend from morning gold into the shared sky; the shared sky lock outranks it).

- **Rubble is the boulder mechanic verbatim** — rows in the newborn's zone id; mining, hit feedback, and the stone drop just work. World-zone tools (`resetBoulders`, the registry seeder, the spawn cap) never touch other zones' rows.
- **The cave is yours and it persists**: nothing is wiped on emerge — your mined-out neck stays mined. In the world, the arrival alcove's deep end is a dark tunnel mouth; pushing into it (`enterCave`, same client-detect/server-verify shape) descends into your own cave, landing below the neck so arriving never bounces straight back out. Every trogg keeps its cave; nobody else's is reachable.
- The client boots the zone named on the player row (a newborn's cave or the world); `getZone` resolves any `birth:*` id to the shared template. Terrain palettes use regions only on the world map — other zones are one biome throughout.
- Deaths respawn just outside your cave (the coast alcove, `EMERGE_ARRIVAL`), including a newborn that dies mid-dig — the cave is for births, not a spawn room.
- Open threads: the emergence sightline (a landmark fire at town, walk-to-town breadcrumbs, naming at the fire).

### Debug cheats (Commands drawer)

Developer/alpha-tester tools in the Commands drawer (a lone top-right toggle — labelled **Debug Tools** — opening a right-edge drawer; the toggle rides the drawer's edge so the same button closes it), gated by the `cheat-commands` flag. They exist both for debugging and so alpha testers can feel the game out and escape any weird spot they lock themselves into. Cheats live on the player row (`cheatSpeed`/`cheatFly`/`cheatNoclip`/`cheatInvulnerable`, plus the flight motion columns `z`/`dirZ`) and are written only by dedicated reducers that **settle motion first** (a rules change must never replay elapsed motion history at new rules) and clamp every value (invariant 3).

- **Speed ×3** (`CHEAT_SPEED_MULTIPLIER`, 3 *(initial)*): multiplies the shared projection's tiles-per-second; rides the row like `running`, so every client derives the same faster motion.
- **Fly:** airborne, with real synced altitude. `z` is a linear derivation origin like `x`/`y` and `dirZ` the vertical intent, written on **Space/C input transitions** (`setLift` — the `move` pattern's third axis, invariant 2): Space climbs, C sinks, release holds, clamped to `FLY_MAX_HEIGHT` (14 *(initial)*) at `FLY_VERTICAL_TILES_PER_SEC` (5 *(initial)*). Horizontal collision is **height-aware**: a flyer clears whatever sits below it — deep water above `FLY_CLEAR_WATER` (0.2), trees/boulders/creatures above `FLY_CLEAR_OBSTACLE` (3), rock walls above `FLY_CLEAR_ROCK` (11) — judged at the motion's origin altitude, which every input transition and per-tile rebase refreshes. Every client renders a flyer at its true derived altitude.
- **Noclip:** the grounded clearance — walk through anything; the projection keeps only the zone rectangle.
- Switching fly or noclip **off** while inside geometry settles the trogg to the nearest standable tile; landing zeroes `z`/`dirZ`.
- **God mode:** `damagePlayer` no-ops on the target; the attacker's swing still lands and animates.
- **Heal** (`healSelf`): full health for a living trogg — respawn already handles the dead.
- **Unstuck** (`rescue`): settle, ground, and step to the nearest standable tile (spawn as the last resort) — the universal escape hatch.
- **Sky lock (World section):** a slider pins the day-night phase for **everyone** — the sky is shared fiction, so a scrubbed noon is noon on every client. The override lives in the public `world_state` singleton (id 0, `skyLocked`/`skyPhase`), written by `setSky` (phase wrapped into [0, 1)); "Live" releases it back to the shared wall clock. Clients subscribe to the row and read it in their daylight pass; the drawer's controls paint from it, so every open drawer agrees.

### Crafting

- Recipes: inputs → output, optional skill/level requirement, crafted at stations in the Hog town (e.g., torches, better picks).
- Better tools gather faster or unlock harder node types — the progression spine.

### Communal construction

- Projects the whole tribe contributes resources to ("the beacon needs 500 stone"), with a public progress bar. Completion unlocks a zone, station, or capability for everyone.
- Contributions are per-player tracked (leaderboards, achievements); the unlock is shared.

### Hog merchants and economy

- Hog NPCs can buy and sell at fixed prices in town. Player trading, if added, is a transactional mutation under contention — both inventories checked and mutated atomically.
- Currency: TBD (working name: shards).

### Defense events

- Things come out of the dark; the tribe defends the Hogs. Event-based PvE combat: scheduled waves, slow stat-driven resolution, click-to-engage. No projectiles, no physics, no real-time aiming — ever (invariant 7).

### AI inhabitants

- LLM-driven Hog NPCs with real dialogue (merchants, quest-giver-ish townsfolk). Every interaction is an LLM call traced with AI observability.

## Data model

One layer. **SpacetimeDB** is the durable store *and* the live feed: the tables below are the source of truth, and clients subscribe directly to the rows in their current zone — there is no separate cache or room projection to keep in sync. Only trusted module entrypoints may write: reducers for pure game mutations and procedure wrappers that open a SpacetimeDB transaction with `ctx.withTx(...)` before doing external telemetry. The writer's identity is the connection's own `ctx.sender` (invariant 3). A connecting client upserts its `player` row (`clientConnected`) and settles it on disconnect (`clientDisconnected`); SpacetimeDB persists every table, so a returning Identity resumes its trogg with no hydrate step. Motion intents (direction, `movedAt`) live in the durable row, but position is still *derived* from them with `projectMotion`, never advanced on a timer — the no-teleport-by-quit rule holds (invariant 1). A zone with no connected players produces no diffs and no work. Indexes noted where the access pattern demands them.

Dev mirrors prod: a local `spacetime start` instance runs the very module production runs — `just dev` publishes to it and regenerates the client bindings — so persistence is exercised the same way it runs in production. No Docker, no separate database to provision.

```text
player         identity (PK), name, isGuest, zoneId, x, y, dirX, dirY, movedAt, online, lastChatAt, running, color, carrying, path, style, equippedMainHand, equipmentAction, equipmentActionAt, equippedMainHandInventoryId, faceX, faceY, carryingStyle, health, dead, respawnAt, equippedOffHand, equippedOffHandInventoryId
               keyed by the connection's Identity. motion derived from origin (x,y) + movedAt: WASD uses
               dirX/dirY (0,0 = idle); running (shift held) picks run speed over walk speed in projectMotion,
               so it rides the intent like direction; click-to-move stores `path` as serialized waypoint tiles
               (`"x,y;x,y;..."`, empty = no path). online: in-zone
               presence — clients subscribe to online players, so a disconnect settles the row and drops it
               from view without losing progress. lastChatAt: per-player chat rate limit. color: chosen
               TROGG_COLORS palette index (COLOR_UNSET = -1 → colour derived from id; see "Avatars").
               carrying: kind of tile-sized entity the trogg holds ("" = none), set by `interact`; the held
               entity's own row is removed while carried and re-inserted on put-down (see "Interacting").
               carryingStyle: carried entity visual variant; currently used only for Hog skins so carrying
               and putting down a Hog preserves its look even though the Hog row is replaced.
               style: chosen TROGG_STYLES index (STYLE_UNSET = -1 → style derived from id; see "Avatars"),
               set by `restyle`. equippedMainHand/equippedOffHand: equipped item id per hand ("" = none);
               equippedMainHandInventoryId/equippedOffHandInventoryId: the specific owned inventory row
               equipped in that hand (0 = none).
               equipmentAction/equipmentActionAt: last synced use impulse for animation. faceX/faceY:
               standing facing, separate from movement intent so idle turns sync without deriving position.
               health/dead/respawnAt: combat state; dead troggs stay online but cannot act while
               the respawn countdown is active. `player_respawn` returns them to spawn after `respawnAt`.
               Columns are appended in migration order (the off-hand pair is the current tail), so the
               shipped layout is never reordered; see module source.
               index: by_zone (zoneId)
zones          slug, name, width, height, tilemap (per-tile walkability + scenery), checkpoint (unlock tile, null if none)
               index: by_slug (slug)
               deferred — zone definitions currently live in a static code registry (ZONES in shared); this table lands when tilemaps need editable storage
nodes          type, zoneId, x, y, state ("available" | "depleted"), respawnAt
               index: by_zone (zoneId)
boulder        id (PK, auto-inc), zoneId, x, y, health     (tile coords)
               a mineable rock on an unwalkable tile; clients subscribe per zone and treat it as a
               dynamic obstacle. Seeded from the ZONES registry on first connect; a pickaxe mines it
               into Stone (deleting the row), and the `resetBoulders` reducer (Commands panel) reseeds
               the registry layout.
               index: by_zone (zoneId)
tree           id (PK, auto-inc), zoneId, x, y, health     (tile coords)
               a choppable tree — the woodcutting mirror of the boulder: same dynamic-obstacle
               collision, seeded from the ZONES registry, felled into Wood by an axe (deleting the
               row). Not carryable.
               index: by_zone (zoneId)
hog            id (PK, auto-inc), zoneId, x, y, dirX, dirY, movedAt, path, homeX, homeY, style, health, lastDamagedAt
               an ambient roaming Hog NPC (see "Hogs"). Intent-based motion like a player (position
               derived with projectMotion); server-owned, no identity. Solid: blocks troggs and other
               Hogs (troggs never block each other), so wanderHogs re-bases it tile by tile, stopping
               flush against anything solid. dirX/dirY is its cardinal amble heading; path/homeX/homeY are
               retained columns from the earlier pathfinding wander, unused by the tile-by-tile amble and
               kept only so the shipped schema isn't reordered. style: "" = common roamer (id-derived
               skin); "buff"/"dino" = a 2×2 showpiece; "chicken" = easter egg; or any concrete HOG_STYLES
               id for exact Commands panel spawns and carried-and-dropped Hogs preserving their look.
               hogSize(style) gives the footprint span (appended last per the migration note). Seeded from
               the ZONES registry on first connect, spawned by the Commands panel, moved only by the scheduled
               `wanderHogs` (or reset to the registry population by the `resetHogs` reducer, fired by the Commands panel).
               `health` makes Hogs damageable; zero health deletes the row. Removed while a trogg carries
               it and re-inserted on put-down (see "Interacting").
               index: by_zone (zoneId)
ground_item    id (PK, auto-inc), zoneId, item, x, y, qty
               a pickup item lying on the floor. Seeded from the ZONES registry on first connect, spawnable one at
               a time from the Commands panel only when its id is in SPAWNABLE_ITEM_IDS, and removed by `interact`
               when a trogg faces it and presses `E`. `qty` preserves dropped stack counts when a dead trogg
               drops inventory. Items are not solid. index: by_zone (zoneId)
inventory      id (PK, auto-inc), playerId, item, qty
               player-owned items. Each row occupies one of 10 visible carry slots. Stackable rows merge
               and can grow in-place; starting a new stack or adding a non-stackable item requires a free
               slot. Non-stackable equippables stay separate qty=1 rows so equipment can point at one
               specific owned row. index: by_player (playerId)
player_connection connectionId (PK), playerId, connectedAt
               private live-socket presence for a `player` row. Multiple browser tabs or devices signed
               into the same account share one durable `player` row, so this table tracks the connection
               ids behind that identity. `clientConnected` inserts the socket; `clientDisconnected`
               removes it and only settles/drops/marks the trogg offline when no connections remain.
               index: by_player (playerId)
player_respawn scheduledId (PK), playerId, scheduledAt
               private one-shot scheduled rows inserted on trogg death. The scheduled `respawnPlayers`
               reducer re-checks `player.dead` and `player.respawnAt`, then returns the same `player`
               row to zone spawn at full health while preserving name, colour, style, and identity.
hog_wander     scheduledId (PK, auto-inc), scheduledAt     (scheduled table)
               the Hog wander timer — SpacetimeDB's deterministic scheduler (invariant 1). Fires
               `wanderHogs`, which re-arms it only while a player is online. Private (no client reads it).
ghost_haunt    id (PK, auto-inc), zoneId, x, y, createdAt
               a zone-scoped cosmetic fanout event for the ghost. `hauntGhost` chooses a random
               walkable tile server-side, inserts the row, and trims each zone to
               GHOST_HAUNT_HISTORY_MAX. Clients subscribe per zone and render fresh inserts only,
               so everyone already in the map sees it once and late joiners don't replay old haunts.
               index: by_zone (zoneId)
actions        playerId, nodeId, kind, startedAt, endsAt
               index: by_player (playerId)
chat_message   id (PK, auto-inc), zoneId, sender (Identity), name (denormalised), text, createdAt
               a new row is the live bubble; clients subscribe to recent rows per zone, capped at
               CHAT_HISTORY_MAX (trimmed in the chat reducer). `rename` rewrites `name` across the
               sender's rows so history tracks the current name. index: by_zone (zoneId)
claim_code     code (PK), guest (Identity), createdAt
               a pending guest → account claim (see "Identity"). Private (not public): the nonce lives
               only in the browser that minted it; no client reads this table. startClaim writes it under
               the guest Identity; redeemClaim consumes it as the account. Stale after CLAIM_CODE_TTL_MS.
skills         playerId, skill, xp
               index: by_player (playerId)
projects       slug, zoneId, status, requirements, contributed
               index: by_zone (zoneId)
```

## Multiplayer scaling stance

- **One subscription per zone, naive within it.** A client subscribes to the rows of its current zone; SpacetimeDB pushes row diffs to everyone subscribed. Within a zone there's no interest management and no instancing — everyone sees every player.
- **Fixed cost, watched in metrics.** The VPS runs the self-hosted SpacetimeDB instance on a flat monthly bill, not a usage meter, so the concern is CPU, memory, and bandwidth — watched via server metrics and PostHog, not an egress invoice. At the realistic scale (tens of concurrent) a single instance on a small Hetzner box is ample.
- **Capacity cap before launch.** Connection and subscription limits are set so a viral night sheds or queues load instead of toppling the box; vertical scale (a bigger VPS) is the first lever. Per-zone entity caps (`MAX_HOGS_PER_ZONE`, `MAX_BOULDERS_PER_ZONE`, `MAX_GROUND_ITEMS_PER_ZONE`) bound Commands panel spawns and carried-object drops server-side, so no client can flood a zone and inflate every `wanderHogs` tick.
- **The answer key, only when a graph demands it:** narrow the subscription with area-of-interest SQL filters (a smaller `WHERE` per client), crowd aggregation above a density threshold, and — when one box isn't enough — SpacetimeDB's own horizontal scaling. None of these are built in advance.
- **Same module, dev to prod.** The instance the VPS runs is the module that runs locally, mirrored from the current production module. Running beyond a single instance — and everything else in the answer key — stays deferred under invariant 10.
- **Swappable position feed:** the client reads positions through the zone subscription and writes through reducers, both in one place. Tightening to area-of-interest is a query change, not a rewrite.

## Invariants (non-negotiable)

1. No simulation tick. We never run a server simulation loop; state changes only inside a reducer — on player input or a scheduled reducer (SpacetimeDB's deterministic timer, used for respawns and action completions). SpacetimeDB sends only row diffs to subscribers, so an unchanged table produces none and a zone with no connected players computes nothing.
2. No per-frame or per-tile server sync. Movement intents (click→path, key down/up, direction change) are sent via reducers; clients derive and predict motion locally. Synced player state changes on input, never on a timer or every frame.
3. All authoritative state lives on the server — SpacetimeDB's durable tables, written only by reducers or procedure transactions, with the writer identified by `ctx.sender`. Never trust the client.
4. Analytics events are low-volume and never contain chat content or PII beyond the player name (full rules in [analytics.md](analytics.md)).
5. Feature flags are operational controls, not a blanket requirement. Add or configure one when a feature needs remote rollout, a kill-switch, an experiment, or live tuning; otherwise keep the code simple. Any flag key the code reads must be registered in [analytics.md](analytics.md) with its fallback behavior, and the matching PostHog flag must be created or updated in the configured project during the same task.
6. The game is playable at the end of every session. No half-wired states on `main`.
7. No twitch combat: combat is tile-based, stat-driven, and slow — no projectiles, physics, real-time aiming, or per-frame attack checks. No procedural generation.
8. No secrets in the repo — env vars only, `.env*` is gitignored. This repo is public.
9. Glossary names are canonical across code, schema, events, and UI.
10. No preemptive scaling work. Optimizations from the scaling answer key are built only when a dashboard graph justifies them. No instancing below ~1,000 concurrent in a zone.

## Roadmap and current state

Roadmap notes are planning context, not permission gates. Pick work by current product need, maintainer direction, and what keeps the game playable. Do not block a task because older notes placed it later, and do not treat this section as a release checklist.

Current playable foundation: durable SpacetimeDB tables are the store, anonymous SpacetimeDB Identity gives each browser a persistent trogg, and optional SpacetimeAuth OIDC lets a guest claim an account with `startClaim`/`redeemClaim`, `rename`, `player_named`, and `posthog.identify()`. Identity is issued by the connection and module entrypoints authorize by `ctx.sender`; it is never client-asserted. Multiple live sockets for the same account share one trogg and are tracked through private per-connection presence.

Implemented world systems: a static shared `ZONES` registry, zone-scoped subscriptions, per-tile walkability, free camera-relative WASD movement with sliding collision, boulder mining (pickaxe → Stone), tree felling (axe → Wood), pick-up-and-carry interaction (`E`, Hogs), throwable Hogs, starter tool pickups, inventory/equipment with `I`, equipped-item use with `F`, sword attacks, trogg and Hog health, death drops, timed respawn, roaming Hogs, hold-shift-to-run, 3D avatars, trogg recolouring/restyling via Appearance (`P`), chat bubbles/history, a small synced ghost-trogg cosmetic (Commands panel summon), a help panel listing live controls, and a pre-alpha Commands panel for one-click spawn/reset/ghost tools. Some of these have optional client-side flag gates for remote rollout or kill-switch use; the current code-read flags are configured in PostHog and still have code fallbacks for local or unconfigured environments.

Likely next work areas include starting-zone onboarding, click-to-move pathfinding around obstacles, gathering and XP, crafting, communal projects, Hog merchants, load events, LLM-driven Hogs, and optional PvE defense. These are intentionally fluid; implement the slice that best serves the current task.

## Open design threads

- Mascot integration: the trogg/Hog cast renders as code-built jointed 3D models (`src/game/creatures.ts`), with rig-driven limbs, the attack swing, and held main/off-hand items parented to the hands. HUD icons render from the same models. Further work is polish: richer clips, the attachment armour/cosmetic stack (see "Layered avatars and cross-species equipment"), and any artist-authored model pipeline (scripted Blender → glTF) if the project outgrows code-built geometry.
- Zone and terrain art direction once the plain cave stops being charming.
- Light/darkness as a mechanic: torch radius, communal beacons, dark-gated zones — how literal to make it.
- Own-avatar prediction polish: basic optimistic acknowledgement handling has landed for movement; richer rollback/smoothing is deferred until playtests show a need.
- Currency design.
